<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <title>Happy Birthday 💗</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    body {
      margin: 0;
      height: 100vh;
      background: #ffe6f0;
      font-family: 'Poppins', 'Comic Sans MS', cursive;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .wrapper {
      width: 100%;
      max-width: 390px;
      height: 100vh;
      background: linear-gradient(180deg, #ffd1e8, #fff6fb);
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    .slide {
      position: absolute;
      text-align: center;
      opacity: 0;
      transform: translateY(40px);
      transition: 0.8s ease;
      padding: 20px;
    }

    .slide.active {
      opacity: 1;
      transform: translateY(0);
    }

    h1, h2, p {
      color: #ff4f9a;
    }

    p {
      font-size: 16px;
      line-height: 1.6;
    }

    .kitty {
      font-size: 72px;
      margin-bottom: 10px;
      animation: kittyBounce 1.6s infinite;
    }

    @keyframes kittyBounce {
      0%,100% { transform: translateY(0); }
      50% { transform: translateY(-18px); }
    }

    .tap {
      position: absolute;
      bottom: 30px;
      font-size: 13px;
      color: #ff77b7;
      animation: blink 1.4s infinite;
    }

    @keyframes blink {
      0%,100% { opacity: 1; }
      50% { opacity: 0.4; }
    }

    .heart {
      position: absolute;
      bottom: -20px;
      color: #ff6fb1;
      font-size: 18px;
      animation: floatUp linear forwards;
      opacity: 0.8;
    }

    @keyframes floatUp {
      to {
        transform: translateY(-120vh);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

<div class="wrapper" onclick="nextSlide()">

  <div class="slide active">
    <div class="kitty">🐱</div>
    <h1>hi sayang</h1>
    <p>aku bikin ini khusus buat kamu 💗</p>
  </div>

  <div class="slide">
    <h2>happy birthday 🎂</h2>
    <p>semoga hari ini seimut senyummu</p>
  </div>

  <div class="slide">
    <h2>thank you for existing 💕</h2>
    <p>aku bersyukur banget punya kamu</p>
  </div>

  <div class="slide">
    <h2>🎁 surprise</h2>
    <p>pelukan, doa, dan semua rasa sayangku 💖</p>
    <div class="kitty">😽</div>
  </div>

  <div class="tap">tap anywhere 💕</div>
</div>

<script>
  const slides = document.querySelectorAll('.slide');
  let current = 0;

  function nextSlide() {
    slides[current].classList.remove('active');
    current = (current + 1) % slides.length;
    slides[current].classList.add('active');
  }

  function spawnHeart() {
    const h = document.createElement('div');
    h.className = 'heart';
    h.innerText = '❤';
    h.style.left = Math.random() * 100 + 'vw';
    h.style.animationDuration = (Math.random() * 3 + 4) + 's';
    document.body.appendChild(h);

    setTimeout(() => h.remove(), 7000);
  }

  setInterval(spawnHeart, 500);
</script>

</body>
</html>
